---
title: PaperPlane.cc 私有 Docker 镜像仓库
type: docker
repo: registry
---

镜像仓库 URL： `https://docker.p01.cc`
此镜像仓库无 UI，浏览器访问 URL 没有页面展示。

登录方式：

```bash
docker login docker.p01.cc
```

随后输入用户名和密码，成功登录时会提示 “Login Succeeded”；
私有仓库镜像的推送与拉取，请使用 `docker.p01.cc/` 前缀。
