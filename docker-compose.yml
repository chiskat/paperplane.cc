# 本地运行专用 docker compose 文件
services:
  paperplane-cc-local:
    image: chiskat/baseline-node24-puppeteer:2026.6.8
    container_name: paperplane-cc-local
    working_dir: /paperplane.cc
    init: true
    ports:
      - '3000:3000'
    volumes:
      - .:/paperplane.cc
    extra_hosts:
      - host.docker.internal:host-gateway
    command: 'bash -c "pnpm i && pnpm dev"'
